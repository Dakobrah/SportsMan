// Modules index for tracker — attaches references to `window.TrackerModules`
(function () {
    'use strict';
    window.TrackerModules = window.TrackerModules || {};
    // This file intentionally left small — individual modules expose themselves
    // on `window.TrackerModules` so the main `tracker.js` can be migrated later.
})();
