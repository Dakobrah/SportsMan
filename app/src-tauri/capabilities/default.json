{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "SQLite goes through this app's own db_* commands, so no SQL plugin permission is needed. Dialog and text-file access exist solely for backup export/restore.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-save",
    "dialog:allow-open",
    "fs:allow-read-text-file",
    "fs:allow-write-text-file"
  ]
}
